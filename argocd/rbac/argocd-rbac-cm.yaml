apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.default: role:readonly
  policy.matchMode: glob
  scopes: '[groups]'
  policy.csv: |
    p, role:admin, *, *, *, allow
    g, admin, role:admin
