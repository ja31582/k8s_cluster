apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple
  namespace: jano
  labe
  
spec:
  rules:
  - host: foo.com
    http:
      paths:
      - backend:
          service:
            name: svc1
            port:
              number: 8080
        path: /bar
        pathType: Exact
status:
  loadBalancer: {}
