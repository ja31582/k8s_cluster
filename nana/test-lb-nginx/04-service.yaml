apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  namespace: test-lb-nginx
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.90
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
