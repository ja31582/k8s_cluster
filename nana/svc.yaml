apiVersion: v1
kind: Service
metadata:
  labels:
    app:  my-pod
  name: new-cidr
spec:
  ports:
  - name: 8081-80
    port: 8081
    protocol: TCP
    targetPort: 80

  selector:
    app: my-pod
  type: LoadBalancer
  