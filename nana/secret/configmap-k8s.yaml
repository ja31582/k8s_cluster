apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-config
data:
  application.properties: |
    app.name=demo-app
    app.port=8080
  logging.properties: |
    logging.level=INFO
    logging.format=json
